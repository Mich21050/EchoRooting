# Upgrade Reverse to tty
# install pty,tty from python/ftp
python -c 'import pty; pty.spawn("/bin/bash")'

#Ctrl + Z in Host Shell
stty raw -echo

#switch to remote
fg

# install pty,tty from python/ftp
python -c 'import pty; pty.spawn("/bin/bash")'

#execute on remote
export SHELL=bash
